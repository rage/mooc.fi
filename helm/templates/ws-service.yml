apiVersion: v1
kind: Service
metadata:
  name: ws
  labels:
    app: {{ include "helm.fullname" . }}-backend
spec:
  type: NodePort
  ports:
  - port: 9000
    name: "ws"
    targetPort: ws
  selector:
    app: {{ include "helm.fullname" . }}-backend
