apiVersion: v1
kind: Service
metadata:
  name: moocfi-backend
  namespace: moocfi
  labels:
    app: moocfi-backend
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: backend-http
  - port: 9000
    targetPort: ws
  selector:
    app: moocfi-backend
